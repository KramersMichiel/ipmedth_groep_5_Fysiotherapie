import 'package:flutter/material.dart';

class VideoImportPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Video Import Page'),
      ),
      body: Center(
        child: Text('This is the Video Import Page'),
      ),
    );
  }
}
